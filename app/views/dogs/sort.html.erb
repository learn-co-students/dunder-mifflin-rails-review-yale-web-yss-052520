<h1> Sorted Dogs</h1>
<ul>
    <%@dogs.each do |dog|%>
        <li>
            <%=link_to dog.name, dog_path(dog)%> <br>
            Number of employee(s): <%=dog.employees.length%> 
            </li> 
    <%end%>
</ul>

